import styles from './ProfileInfo.module.css';

const ProfileInfo = () => {
    return (
        <main className={styles.main}>
            ava + description
        </main>
    );
}

export default ProfileInfo;
